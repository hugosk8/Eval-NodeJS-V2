extends layouts/admin_layout

block content
    .container.text-center 
        .row 
            h1= title
            canvas#barCanvas(aria-label="chart" role="img")

    script.
        const materialData = !{JSON.stringify(materialData)};

    script(src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.7.1/chart.min.js")
    script(src="/assets/js/graphic.js" type="module")